var nome = 'Luis';

console.log(nome) // Luis
console.log(nome.length) // 4
console.log(nome.toUpperCase()) // LUIS
console.log(nome.toLowerCase()) // luis

var altura = 1.8;

console.log(typeof altura) // number
console.log(typeof altura.toString()) // string

console.log(addEventListener.length) // 2, retorna a qntd de parâmetro da function


// nomei 3 propriedades ou métodos de strings
var btn = document.querySelector('.btn');
btn.classList.add('ativo') // adiciona a classe
console.log(btn)

btn.classList.remove('ativo'); // remove a classe
console.log(btn)
// nomeie 5 propriedades ou métoodos de elementos DOM
btn.addEventListener
btn.appendChild
btn.id
btn.innerHTML
btn.outerHTML

// busque na web um objeto(método) capaz de interagir com o clipboard,
// clipboard é a parte do seu computador que lida com o CTRL + C, CTRL + V